import React,{useEffect,useState}from'react';import{Link}from'react-router-dom';import api,{errorMessage}from'../api/client';import Loading from'../components/Loading';import{currency,dateTime,signedClass}from'../utils/format';
export default function Transactions(){const[items,setItems]=useState([]);const[type,setType]=useState('');const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{setLoading(true);api.get('/trades/transactions',{params:{type,limit:100}}).then(r=>setItems(r.data.data)).catch(e=>setError(errorMessage(e))).finally(()=>setLoading(false))},[type]);return <div className="page-bg"><section className="page-banner compact-banner"><div className="container"><span>Activity</span><h1>Transaction history</h1><p>A complete ledger of your buy and sell orders.</p></div></section><div className="container py-4 py-lg-5"><div className="market-toolbar justify-content-end"><select className="form-select" value={type} onChange={e=>setType(e.target.value)}><option value="">All transactions</option><option>BUY</option><option>SELL</option></select></div>{error&&<div className="alert alert-danger">{error}</div>}<div className="content-panel">{loading?<Loading/>:<div className="table-responsive"><table className="table market-table align-middle"><thead><tr><th>Date & time</th><th>Stock</th><th>Side</th><th className="text-end">Quantity</th><th className="text-end">Price</th><th className="text-end">Total</th><th className="text-end">Realized P/L</th><th>Status</th></tr></thead><tbody>{!items.length&&<tr><td colSpan="8" className="empty-cell">No transactions yet.</td></tr>}{items.map(t=><tr key={t._id}><td>{dateTime(t.createdAt)}</td><td><Link className="security-cell" to={`/stocks/${t.symbol}`}><b>{t.symbol}</b><span>{t.stock?.name}</span></Link></td><td><span className={`trade-side ${t.type.toLowerCase()}`}>{t.type}</span></td><td className="text-end">{t.quantity}</td><td className="text-end">{currency(t.price)}</td><td className="text-end fw-semibold">{currency(t.total)}</td><td className={`text-end ${signedClass(t.realizedProfitLoss)}`}>{currency(t.realizedProfitLoss)}</td><td><span className="status-dot"><i/> {t.status}</span></td></tr>)}</tbody></table></div>}</div></div></div>}
